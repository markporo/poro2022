module.exports = {
  reactStrictMode: true,
  images: {
    // domains: ['your-image-source-domain.com'], // Add any domains from which you're loading images
   // deviceSizes: [320, 420, 768, 1024, 1200], // Set the device sizes for responsive images
    //imageSizes: [16, 32, 48, 64, 96], // Custom image sizes
    formats: ['image/webp', 'image/avif'], // Enable serving images in modern formats
  },
  // output: 'export',
}
